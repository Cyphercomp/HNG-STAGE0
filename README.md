# Gendirize Api
  This Api Fetch data from https://api.genderize.io?name=name passing name as a parameter 
  it then compute if the prediction is confident uing probability and count.

## Endpoint 
  GET : api/classify/?name=peter
  
## Stack
  Django
  Django-rest-framework
  Requests